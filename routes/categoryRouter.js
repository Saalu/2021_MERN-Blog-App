// const router = require('express').Router()
// const itemCtrl = require('../controllers/itemCtrl')
// const auth = require('../middleware/auth')

// router.route('/')
//     .get(auth, itemCtrl.getItems)
//     .post(auth, itemCtrl.createItem)

// router.route('/:id')
//     .get(auth, itemCtrl.getItem)
//     .put(auth, itemCtrl.updateItem)
//     .delete(auth, itemCtrl.deleteItem)

// module.exports = router
